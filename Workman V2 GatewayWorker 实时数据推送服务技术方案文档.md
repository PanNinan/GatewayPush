# Workman V2 GatewayWorker 实时数据推送服务技术方案文档

## 1\. 方案概述

### 1\.1 项目背景

本项目基于 Workman V2 版本，依托 GatewayWorker 网关业务分离架构，搭建一套支持**WebSocket\+UDP 双协议**的高性能实时数据推送服务。服务主打单对一定向推送能力，支撑上万级长连接并发，初期单机部署。

架构按**无状态**原则设计，为后期多机集群横向扩容预留了能力；但集群化**并非零改造** —— 经一次针对实现代码的就绪度核查，落地前需完成 §7.2 列出的 **4 项改造**与 **1 项启动校验**，否则会出现节点无法启动、消息静默错投等生产故障。核查结论已按实际差距修订至 §7.2。

### 1\.2 核心需求汇总

- **通信协议**：同时支持 WebSocket（长连接实时推送）、UDP（轻量化数据上报/推送）

- **核心业务**：实时数据单对一定向推送，无分组广播需求

- **并发能力**：支持上万客户端同时在线连接

- **数据存储**：仅依赖 Redis，用于会话存储、设备映射、临时缓存、监控指标存储

- **架构模式**：GatewayWorker 解耦架构（网关进程\+业务进程分离）

- **基础能力**：连接Token鉴权、断线重连与会话保持、心跳检测、死连接自动清理、日志收集、异常捕获、自定义定时任务

- **运维能力**：支持优雅重启、完整服务监控指标采集

- **版本兼容**：兼容 PHP8\.0 \- PHP8\.5 全版本

- **部署规划**：初期单机部署；架构无状态化，为后期多机集群横向扩容预留能力。集群落地需按 §7.2 完成节点隔离与唯一性校验改造（**非零改造**）

### 1\.3 方案优势

- **解耦清晰**：网关层负责网络底层，业务层负责逻辑处理，互不干扰，稳定性极高

- **高性能**：事件驱动、常驻内存、多进程模型，适配上万并发场景

- **可扩展**：全量会话数据与映射关系依托Redis，服务节点无业务状态绑定，为横向扩容提供架构基础。**注意**：集群落地仍需完成节点隔离（runtime 路径、内部通信端口唯一性）与启动校验，详见 §7.2，不可理解为开箱即用

- **高可用**：支持平滑优雅重启，发布服务不中断在线连接

- **易运维**：内置完整日志、异常捕获、服务指标监控，问题可追溯

## 2\. 技术选型

|技术组件|版本/说明|用途|
|---|---|---|
|核心框架|Workman V2 \+ GatewayWorker|实现多进程事件驱动、协议监听、网关业务分离|
|运行环境|PHP8\.0 \~ PHP8\.5|向下兼容低版本，适配主流服务器PHP环境|
|数据缓存|Redis 稳定版|存储会话映射、设备绑定、临时缓存、监控指标|
|通信协议|WebSocket \+ UDP|WebSocket承载长连接推送，UDP承载轻量化无状态数据交互|
|数据交互|JSON统一报文格式|统一双协议数据格式，简化解析与编解码逻辑|



## 3\. 整体架构设计

### 3\.1 架构模型

采用**Gateway \+ BusinessWorker 分布式解耦架构**，完全区别于单进程模型，适配高并发、后期集群扩容场景，两层进程职责完全隔离：

#### 3\.1\.1 Gateway 网关进程（网络层）

- 同时监听 WebSocket、UDP 双端口，负责所有客户端网络连接接入

- 职责：连接建立、底层心跳检测、死连接清理、原始报文接收与转发、连接状态管理

- **无任何业务逻辑**，仅做网络调度，保证网络层极致稳定

- 适配UDP无连接特性，自定义应用层会话识别规则

#### 3\.1\.2 BusinessWorker 业务进程（逻辑层）

- 接收Gateway转发的客户端事件与消息，处理所有核心业务逻辑

- 直接操作Redis，完成鉴权、会话管理、消息推送、定时任务等能力

- 实现单对一定向消息推送核心能力

- 承载日志收集、异常捕获、指标统计等运维能力

#### 3\.1\.3 Redis 中心存储层

- 存储 `client_id ↔ 设备ID/用户UID` 映射关系

- 缓存客户端会话信息，支撑断线重连、会话保持

- 存储服务监控指标、运行状态数据

- 为后期多机集群提供统一数据中心，消除本地状态依赖

**集群视角的两点说明**（经代码核实）：

- 在线连接集合（`online:clients`、`online:ws`、`online:udp`）为**集群共享集合**，各节点均向其增删自己的连接，故任意节点执行 `SCARD` 得到的都是**全局在线数** —— 该指标在集群下天然一致，无需分节点采集再聚合。
- 累加型指标走 `HINCRBY`（多机原子安全）；但**覆盖型**的 `memory_bytes:{pid}` 以进程 pid 为 Hash 字段名，跨节点 pid 空间独立，存在数值碰撞导致相互覆盖的可能，且无法直接聚合集群总内存（详见 §7.2.3）。

### 3\.2 数据流转流程

客户端接入 \& 消息推送完整流程：

1. **连接建立**：客户端发起WebSocket/UDP连接，Gateway接收连接请求

2. **鉴权拦截**：连接建立后，BusinessWorker获取客户端Token，完成合法性校验；鉴权失败直接关闭连接

3. **会话绑定**：鉴权成功，将客户端client\_id与设备/用户ID绑定，写入Redis持久化会话

4. **日常通信**：客户端上行数据由Gateway转发至业务层处理，服务端根据设备ID定向推送消息至指定client\_id

5. **心跳维护**：Gateway层定时检测客户端心跳，超时未响应自动清理僵死连接

6. **断线重连**：客户端断线后重新接入，携带有效Token，从Redis恢复历史会话，重新绑定连接

## 4\. 核心功能详细设计

### 4\.1 双协议兼容能力

#### 4\.1\.1 WebSocket 协议

- 基于长连接特性，维持客户端持续在线状态

- 原生支持onConnect、onMessage、onClose事件，适配完整会话生命周期管理

- 依托Gateway原生心跳机制，高效管理在线连接

#### 4\.1\.2 UDP 协议

- 无连接、轻量化协议，适用于高频小型数据上报、离线推送通知

- 无原生连接状态，自定义**应用层会话规则**：通过报文内Token/设备ID识别客户端身份

- 自定义UDP报文编解码、合法性校验逻辑，保证数据安全

**统一规范**：双协议统一使用JSON报文格式，字段一致，降低业务处理复杂度。

### 4\.2 连接鉴权体系

- **鉴权时机**：客户端连接建立后、首次通信前强制鉴权，未鉴权连接不允许任何业务交互

- **鉴权规则**：校验客户端携带的Token有效性、过期时间、设备合法性

- **异常处理**：Token无效、过期、篡改，直接关闭连接并记录错误日志

- **权限留存**：鉴权成功后绑定会话信息，生命周期内无需重复鉴权

### 4\.3 会话管理与断线重连

- **会话存储**：所有客户端会话数据存入Redis，设置合理过期时间

- **会话保持**：客户端临时断线后，Redis保留会话信息，等待重连

- **断线重连恢复**：客户端重连携带有效Token，自动匹配历史会话，恢复推送权限，无需重新授权

- **会话清理**：长期未重连的过期会话，自动从Redis清除，释放资源

### 4\.4 心跳检测与死连接清理

- 基于Gateway底层实现全局心跳检测，配置统一心跳超时阈值

- 超时未上报心跳的连接判定为僵死连接，自动断开并清理会话数据

- 有效过滤异常掉线、网络中断导致的伪在线连接，节省服务器并发资源

### 4\.5 定时任务能力

- 基于Workman原生Timer定时器实现，常驻内存高精度定时

- 支持多任务独立配置，可实现定时数据清理、指标统计、状态巡检等业务

- 统一封装任务入口，避免定时器泛滥，方便管理维护

### 4\.6 日志与异常捕获

- **分级日志**：区分INFO、WARN、ERROR、DEBUG日志，按天分割存储

- **全链路记录**：记录连接、鉴权、消息收发、定时任务、异常报错全流程日志

- **全局异常捕获**：拦截业务层所有未捕获异常，避免单个异常导致进程崩溃

### 4\.7 服务监控能力

内置指标采集能力，定时统计并写入Redis，支持后续对接监控面板：

- 实时在线连接总数（WebSocket/UDP分别统计）

- 消息收发QPS、成功/失败次数

- 鉴权成功/失败统计

- 进程运行状态、内存占用

### 4\.8 优雅重启能力

- 依托GatewayWorker原生平滑重启机制，执行reload命令实现不停服更新

- 重启过程中，已建立的长连接不中断，不影响用户在线状态

- 仅重启业务进程，网关进程持续提供服务，保障业务连续性

## 5\. 项目目录结构

标准化分层架构，适配开发、维护、扩容需求：

```Plain Text
gateway-push/
├── config/                       # 全局配置目录
│   ├── app.php                   # 基础配置（运行环境、日志、Redis、鉴权、会话、推送、限流、订阅、监控）
│   ├── gateway.php               # 网关配置（Register、WebSocket、UDP、心跳）
│   ├── business.php              # 业务进程、定时任务配置
│   └── actions.php               # 业务动作清单（声明式，接入新动作只改此文件）
├── src/
│   ├── Gateway/                  # 网关层（网络层，无业务逻辑）
│   │   ├── Bootstrap.php         # Register + WebSocket 网关 + UDP 网关装配
│   │   └── UdpProtocol.php       # UDP 协议解析、报文校验
│   ├── Business/                 # 业务层（逻辑层）
│   │   ├── Bootstrap.php         # 业务进程启动、事件注册、UDP/推送队列消费
│   │   ├── Auth.php              # Token 鉴权核心类
│   │   ├── Session.php           # Redis 会话管理、断线重连
│   │   ├── Message.php           # 消息编解码、签名校验
│   │   ├── Router.php            # 指令路由表（cmd 一级 + data.action 二级）
│   │   ├── Push.php              # 单对一定向推送、离线补投、UDP 出站投递
│   │   ├── Subscribe.php         # 订阅关系双向索引（主题 ↔ 用户）
│   │   ├── ActionRunner.php      # 通道无关动作执行器（超时保护、动作级鉴权）
│   │   ├── ActionContext.php     # 动作上下文（身份、参数、回执抑制）
│   │   ├── ActionInterface.php   # 动作处理器契约
│   │   ├── ParamValidator.php    # 参数白名单校验
│   │   ├── Action/               # 业务动作处理器（通用样板）
│   │   ├── Task.php              # 定时任务统一管理
│   │   └── Monitor.php           # 服务指标监控采集
│   ├── Api/
│   │   └── Bootstrap.php         # HTTP 推送接口进程（只入队，不持有连接）
│   └── Common/                   # 公共工具层
│       ├── Logger.php            # 日志、异常捕获封装
│       ├── RedisClient.php       # 兼容 PHP8.0+ 的异步 Redis 客户端
│       ├── RateLimiter.php       # 多维度令牌桶限流
│       ├── Env.php               # 多级 .env 配置读取
│       └── WorkerEvents.php      # Worker 级异常与背压事件绑定
├── tests/
│   └── e2e_check.php             # 双协议端到端自检脚本
├── runtime/                      # 运行时目录（日志、PID、缓存）
├── .env / .env.example           # 环境变量（.env 不纳入版本管理）
├── start.php                     # 项目全局启动入口
└── composer.json                 # 依赖管理配置
```

> **集群化后的结构调整**（详见 §7\.2）：`runtime/` 需按节点隔离为 `runtime/{NODE_ID}/`；新增 `scripts/`（集群验证与编排脚本）与 `deploy/`（部署配置）目录。

## 6\. 版本兼容方案（PHP8\.0\~8\.5）

- **语法兼容**：禁止使用PHP8\.1\+独有枚举、只读属性等语法，统一使用8\.0兼容写法

- **依赖兼容**：Redis客户端、工具类全部适配PHP8\.0最低版本特性

- **类型适配**：弱化强类型严格校验，兼顾高低版本语法差异

- **环境检测**：项目启动自动检测PHP版本，非法版本直接提示终止启动

## 7\. 部署与扩容方案

### 7\.1 初期单机部署

单服务器部署完整 Gateway \+ BusinessWorker \+ Redis 服务，满足上万并发基础需求，部署简单、运维成本低。

### 7\.2 后期集群扩容（预留能力）

> **本节已按针对实现代码的就绪度核查结论修订。**
> 原文「项目零改造支持多机集群扩容」的表述**不准确**：无状态架构基础确实成立，但节点隔离与唯一性校验存在实际缺口，配错即产生静默故障。以下按「已就绪 / 需改造 / 已知次要问题 / 部署形态」四段给出准确结论。

#### 7\.2\.1 架构基础（已就绪，集群化后无需改动）

| 能力 | 实现方式 | 集群可用性 |
|---|---|---|
| clientId 全局唯一 | `Context::addressToClientId()` = `bin2hex(pack('NnN', 内网IP, 内部端口, 连接序号))`，**天然内嵌节点地址** | ✓ 各节点不冲突，且 `sendToClient` 可据此精确寻址到所属 Gateway |
| 会话 / 设备映射 | 全量存 Redis 中心，节点无本地业务状态 | ✓ |
| 在线连接数采集 | `online:clients` 等为共享集合，`SCARD` 返回**全局**在线数 | ✓ 各节点采集结果天然一致 |
| 累加型指标 | `HINCRBY` 原子累加 | ✓ 多机安全 |
| 队列消费 | `popBatch` 原子批量弹出 | ✓ 不会重复消费 |
| 业务进程扩容 | 独立无状态，可水平增加 | ✓ |

#### 7\.2\.2 需改造项（4 项，落地前必须完成）

**A. runtime 目录未按节点隔离 —— 单机模拟多节点的首要阻塞点**

`config/app.php` 中 `runtime_path` / `log_path` / `pid_path` 三者均硬编码为 `$basePath . '/runtime'`，**无节点维度**；配套表现为 pid 文件名 `workerman_{role}.pid`、日志名 `{role}_{YYYY-MM-DD}.log`（二者均按角色区分，另设跨角色错误汇总通道 `error_{YYYY-MM-DD}.log`）。集群改造时若这两个目录未按节点隔离，多节点的同名文件将在共享存储上互相覆盖。

- **后果**：同机启动第二个节点会因 pid 文件冲突被判定「已在运行」而**拒绝启动**；即便绕过，多节点日志也混写同一文件，无法按节点排障。
- **改造**：引入 `NODE_ID`，runtime 三路径改为 `$basePath . '/runtime/' . NODE_ID`；启动入口增加 `--node=` 参数；pid 文件纳入节点维度。

**B. 内网通信端口与 IP 无唯一性校验 —— 配错即静默错投**

`config/gateway.php` 已预留 `websocket.lan_ip` 与 `websocket.start_port`（注释亦写明「多机需错开」），网关装配时将其赋给 Gateway，但**全流程无任何运行时校验**。

- **风险机理**：§7.2.1 中「clientId 天然全局唯一」的成立前提，正是各节点的 `lan_ip` + `start_port` 组合在集群内唯一。若两台节点配置相同，clientId 将发生**跨节点碰撞** —— `sendToClient` 按 clientId 解出的地址会指向错误的节点，消息**静默错投且无任何报错**，是集群化最难排查的故障。
- **改造**：环境自检（`start.php check`）增加「`lan_ip:start_port` 集群内唯一性」校验 —— 启动时将本节点组合注册到 Redis（短 TTL 心跳键），检测到冲突则**拒绝启动并明确报错**。

**C. UDP 出站队列为单条共享 List —— 跨节点源地址存在风险**

入站 `queue:udp:in` 与出站 `queue:udp:out` 均为单一 Redis List，所有 UDP 网关进程竞争消费。消费侧走 `popBatch` 原子弹出，**功能上可行**（UDP 无连接，任意节点均可 `stream_socket_sendto`）。

- **风险**：出站报文由**消费到该任务的节点**发出，其源 IP 为该节点网卡地址。在多网卡 / NAT / 防火墙按源地址做策略的场景下，回程路由可能不经原入口节点，导致客户端收不到或响应异常。
- **改造（建议前者）**：
  1. **节点亲和**：出站队列按节点分键（`queue:udp:out:{NODE_ID}`），网关只消费本节点队列，保证入口与出口节点一致；
  2. **显式绑定源地址**：保持共享队列，消费时按报文记录的入口节点信息绑定源地址发送。

**D. 定时任务 `scope=first` 每节点各自注册 —— 存在重复执行**

当前 `scope=first` 的语义是「**每机** worker 0 注册」，集群下每个节点都会执行一次：

| 任务 | 集群下影响 |
|---|---|
| UDP / 推送队列消费 | 靠 `popBatch` 原子弹出，**安全** ✓ |
| 心跳超时巡检、会话过期扫描、日志清理 | 重复执行，逻辑幂等但浪费资源；多节点同时全量扫描会放大 Redis 压力 |

- **改造**：必全局唯一的任务引入分布式锁（可复用现成的 `setNxEx` 幂等原语），或改为按节点取模分片执行。

#### 7\.2\.3 已知次要问题（可接受，建议记录）

**`memory_bytes:{pid}` 以进程 pid 为 Hash 字段名**：各节点 pid 空间相互独立，存在 pid 数值碰撞导致互相覆盖的可能；且该字段按进程粒度上报，无法直接聚合出集群总内存。若需集群内存总览，应改为 `memory_bytes:{NODE_ID}:{pid}` 或另设聚合指标。

> **附：一条已排除的疑点**。曾怀疑「在线数 `conn_total` 在多机下互相覆盖、只等于最后一台节点的值」，经核实 **该判断不成立** —— `Session::countOnline('')` 执行的是 `SCARD online:clients`，而 `online:clients` 是共享集合，各节点算出的均为全局值，覆盖写入的值相同，不产生偏差。此处记录以免后续重复排查。

#### 7\.2\.4 部署形态与验证要点

- **Register 独立部署**：注册中心为单点，`REGISTER_LISTEN` 已支持改为远程地址，需独立部署并验证故障重连。
- **L1 限流配额放大**：`ip` 维度为**网关进程内内存桶**（零 IO，仅 UDP 网关使用），集群下实际配额 = 配置值 × 节点数 × 每节点 UDP 进程数。`conn` / `uid` / `ping` 维度走 Redis 桶，天然全局，不受影响。
- **验证脚本**：当前无 `scripts/` / `deploy/` 目录，需新增 `tests/cluster_check.php`（集群用例）与节点编排脚本；现有单机端到端脚本保持不动，作为单机回归基线。
- **单机无法覆盖、须留到 Linux 多机验收的边界**：真实跨机 NAT 源地址校验、防火墙策略、内网 RTT、负载均衡器分发（LVS / Nginx stream）、Redis 主从故障切换、ulimit 与内核参数（Windows 无此限制）。

#### 7\.2\.5 建议推进顺序

`NODE_ID` 化 runtime 与 pid → `lan_ip:start_port` 唯一性校验 → UDP 出站队列节点亲和 → 全局唯一任务分布式锁 → 集群验证脚本与部署编排。

## 8\. 核心风险与规避方案

### 8\.1 UDP无连接风险

风险：UDP无连接、无状态，无法复用WebSocket会话机制
规避：自定义应用层设备ID\+Token鉴权体系，独立维护UDP会话识别规则

### 8\.2 高并发阻塞风险

风险：同步Redis操作阻塞事件循环，导致上万并发卡顿
规避：全程使用Workman异步Redis客户端，杜绝同步IO阻塞

### 8\.3 连接数超限风险

风险：上万长连接触发系统文件句柄限制
规避：文档标注生产环境必须调整ulimit文件句柄上限，优化进程数量配置

### 8\.4 版本兼容风险

风险：高版本语法在PHP8\.0环境报错
规避：统一编码规范，启动环境校验，全功能适配低版本特性

### 8\.5 集群 clientId 碰撞风险（静默错投）

风险：集群各节点的 `lan_ip` \+ `start_port` 组合重复时，clientId 会发生跨节点碰撞，`sendToClient` 按 clientId 解出的地址指向错误节点，消息**静默错投且无任何报错**，排查成本极高
规避：启动自检增加「`lan_ip:start_port` 集群内唯一性」校验，冲突即拒绝启动（详见 §7\.2\.2\-B）

### 8\.6 集群 runtime 目录冲突风险

风险：runtime 路径与 pid 文件名均未按节点隔离，同机启动第二个节点会被判定「已在运行」而拒绝启动，多节点日志混写同一文件
规避：引入 `NODE_ID`，runtime 三路径与 pid 文件纳入节点维度（详见 §7\.2\.2\-A）

### 8\.7 集群 UDP 源地址风险

风险：UDP 出站队列为单条共享 List，任意节点均可能消费并发出，源 IP 与入口节点不一致，在多网卡 / NAT / 防火墙按源地址做策略的场景下可能丢包
规避：出站队列节点亲和（`queue:udp:out:{NODE_ID}`），或消费时显式绑定源地址（详见 §7\.2\.2\-C）

### 8\.8 集群定时任务重复执行风险

风险：`scope=first` 语义为「每机 worker 0 注册」，集群下各节点重复执行心跳巡检、会话扫描等任务，浪费资源并放大 Redis 压力
规避：队列消费类依赖 `popBatch` 原子性已天然安全；必全局唯一任务引入分布式锁或按节点取模分片（详见 §7\.2\.2\-D）

## 9\. 开发优先级规划

### P0 基础骨架（必做）

项目架构搭建、双协议监听、Redis封装、基础鉴权、心跳检测、会话存储

### P1 核心业务

断线重连、单对一定向推送、消息编解码、全局日志异常捕获、定时任务实现

### P2 运维扩容

优雅重启调试、监控指标采集、生产环境参数调优。

集群适配（按 §7\.2\.5 顺序推进）：`NODE_ID` 化 runtime 与 pid → `lan_ip:start_port` 唯一性校验 → UDP 出站队列节点亲和 → 全局唯一任务分布式锁 → 集群验证脚本与部署编排。

> （注：部分内容可能由 AI 生成）

# ---------------------------------------------------------------------
# 环境配置（含密钥，禁止提交）
# 模板见 .env.example，本机配置用：php start.php env:init
# ---------------------------------------------------------------------
/.env
/.env.local
/.env.*.local
/.env.dev
/.env.test
/.env.prod
!/.env.example

# ---------------------------------------------------------------------
# 依赖
# ---------------------------------------------------------------------
/vendor/

# ---------------------------------------------------------------------
# 运行时产物（日志 / PID / 临时文件）
# ---------------------------------------------------------------------
/runtime/

# ---------------------------------------------------------------------
# IDE
# ---------------------------------------------------------------------
/.idea/
/.vscode/
*.iml
.workbuddy/

# ---------------------------------------------------------------------
# 测试与静态分析产物
# PHPStan 分析缓存位于 runtime/phpstan（已随 runtime/ 一并排除）
# ---------------------------------------------------------------------
/.phpunit.result.cache
/.phpunit.cache/

# ---------------------------------------------------------------------
# 系统
# ---------------------------------------------------------------------
.DS_Store
Thumbs.db
*.log
composer.phar

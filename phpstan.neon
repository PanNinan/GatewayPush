# ---------------------------------------------------------------------
# PHPStan 静态分析配置
#
# 收敛策略：baseline 冻结存量告警 + 新代码零容忍。
#   存量问题记录在 phpstan-baseline.neon，不在业务代码里做无意义的修补；
#   此后新增的告警必须修复，不得追加进 baseline。
#
# 版本约束：phpstan/phpstan ^2.0（要求 php ^7.4|^8.0），与项目 PHP 8.1 目标兼容。
#
# ⚠ level 与 baseline 必须同源：baseline 是用哪个 level 生成的，parameters.level
#   就得是哪个值，否则 PHPStan 会对每条不再命中的条目报
#   `ignore.unmatched (non-ignorable)`，一次就把门禁刷成红色。
#   历史事故：baseline 曾以 level 6 重新生成（362 条），而此处仍是 level 5，
#   结果 351 条 unmatched 直接让 `composer analyse` 失败。
#
# 分级现状：**level 6**（2026-09-22 由 level 5 提升）。
#   生产代码（src / client/src / start.php）已补齐全部 missingType.* 标注 →
#   生产侧 0 errors；2026-09-23 语义清洗修掉全部 8 条历史告警后，
#   `phpstan-baseline.neon` 已清空为 `ignoreErrors: []`（保留文件以维持双 baseline 结构）。
#   测试侧（tests / client/tests）的 273 条 missingType.* + 38 条有意冻结的
#   语义条目（golden 钉桩 / by-ref 误判）在 `phpstan-tests-baseline.neon`，
#   沿用既有「测试噪音单独一份」的设计。
#   提级前的量化：level 6 全量 538 errors / 66 文件，其中 100% 是
#   `missingType.*`（iterableValue 330 / return 167 / parameter 34 / property 7），
#   零语义告警 —— 即 level 6 的增量只是「数组元素类型标注完整性」，不新增逻辑类检查。
# ---------------------------------------------------------------------

includes:
    # ⚠ 下面两行在「提级并重生成测试侧 baseline」时必须临时注释掉，
    #   否则已冻结的条目会先把错误吃掉，生成出来的 baseline 会缺条目。
    - phpstan-baseline.neon
    # 测试侧存量单独一份：只减不增，避免 tests 的噪音混进生产代码的 baseline
    - phpstan-tests-baseline.neon
    # 扩展不做自动加载：本项目未装 phpstan/extension-installer，
    # 必须显式 include 各自的 rules.neon，否则规则一条都不会生效。
    - vendor/phpstan/phpstan-strict-rules/rules.neon
    - vendor/phpstan/phpstan-phpunit/extension.neon
    - vendor/phpstan/phpstan-phpunit/rules.neon

parameters:
    level: 6

    paths:
        - src
        - client/src
        - start.php
        # tests 在列才能让 phpstan-phpunit 的断言/mock 规则真正生效。
        # 只装扩展不分析 tests，等于白装。
        - tests
        - client/tests

    # 分析器缓存置于 runtime（已在 .gitignore 中排除）
    tmpDir: runtime/phpstan

    # -----------------------------------------------------------------
    # 自定义 phpcs 嗅探器（tools/phpcs/Sniffs/）的符号发现
    #
    # 这两个类由 phpcs 自带的 autoload.php 加载 —— squizlabs/php_codesniffer
    # 的 composer.json **没有 autoload 段**，类不在 composer autoloader 里，
    # 于是 tests/Manual/phpcs_business_rules_check.php 一实例化它们，
    # PHPStan 就报 class.notFound（4 条）。
    #
    # 用 scanFiles 而非 paths：只做「符号发现」，不分析文件内容 ——
    # 若放进 paths，嗅探器内部对 PHP_CodeSniffer\* 的引用会变成新告警。
    # -----------------------------------------------------------------
    scanFiles:
        - tools/phpcs/Sniffs/PHP/ForbiddenCallSniff.php
        - tools/phpcs/Sniffs/Common/RedisKeyLiteralSniff.php

    # 项目下限为 PHP 8.2（由 dev 工具链的传递依赖决定：php-cs-fixer 依赖的
    # symfony/* 7.x 要求 >= 8.2；运行时依赖 workerman 5.x 只要求 >= 8.1）。
    # 显式锚定在**下限**而非跟随分析机版本：本机恰为 8.2.9 时二者一致，
    # 但若将来在 8.3+ 上分析，8.3+ 才引入的类型仍会被判为未知类型 ——
    # 这是预期行为，提示不要把高版本文档/语法当作可依赖前提。
    phpVersion: 80200

    # workerman 大量使用动态属性（$worker->onMessage = ...），
    # 不把 phpdoc 类型当作绝对事实，避免制造无意义的告警
    treatPhpDocTypesAsCertain: false

    # -----------------------------------------------------------------
    # phpstan-strict-rules
    #
    # allRules 全开，仅关 4 项与本项目既定设计冲突/有语义风险的规则。
    # 实测（level 5，src+client 侧）：全开 +187 条，而这 3 条占 157 条（84%）；
    # 关掉后其余全部规则只额外报 11 条，属可修的真问题。
    # -----------------------------------------------------------------
    strictRules:
        allRules: true

        # empty($x) 换成 !$x 不是等价改写：empty('0') === true，而 !'0' === false。
        # 全仓 96 处，机械替换会改变运行时语义，必须逐处人工判断 —— 故不启用。
        # 注意：键「是否存在」的判定仍按项目红线用 array_key_exists，不用 empty()。
        disallowedEmpty: false

        # 要求条件表达式严格布尔化。本仓大量 if (!$row) / while ($list) 这种
        # 「数组/集合真值判断」，改法牵动面大而收益有限，故不启用。
        booleansInConditions: false
        booleansInLoopConditions: false

        # $class::$method() 正是 Task 与 Router 的反射分发设计本身
        # （config 声明 → 反射调用），不是误用；开这条等于与设计对抗。
        dynamicCallOnStaticMethod: false

    ignoreErrors:
        # -----------------------------------------------------------------
        # workerman/redis 的 phpdoc 缺陷：其方法签名把回调参数标注为 null 类型
        # （例如 Client::set() 的 @param null $cb），而运行期实际接受 callable。
        # 本项目全程强制「回调式调用」（见 RedisClient 的封装约定），
        # 故此类告警属库侧问题，在此集中声明（不限 path —— 手工脚本直连
        # workerman Client 时也会命中同一缺陷），不污染 baseline。
        # -----------------------------------------------------------------
        -
            identifier: argument.type
            message: '#expects null, (Closure|callable|int) given#'
